;(function(FAPP){
    FAPP.add('init',function(){
        function bindInit(){
            // init ...
        }

        function bindEvent(){
            // bind event
        }

        function setUp(){
            bindInit();
            bindEvent();
        }
        setUp();
    }).init();
})(FLNS.register(''));
